#summary Reference for the xml configuration.
#sidebar TableOfContents

  * KeyStoreConfiguration: 
    * `<crypt:keystore/>` 
    * `<crypt:b64Keystore/>` 
    * `<crypt:defaultKeystore/>` (since 1.0.1)
    * `<crypt:keystoreRegistry/>`
  * KeyConfiguration: 
    * `<crypt:privateKey/>` 
    * `<crypt:publicKey/>` 
    * `<crypt:privateKeyRegistryByAlias/>`
    * `<crypt:publicKeyRegistryByAlias/>`
  * SimpleDigitalSignatures: 
    * `<crypt:signer/>`
    * `<crypt:verifier/>`
    * `<crypt:b64Signer/>`
    * `<crypt:b64Verifier/>`
  * DigitalSignaturesByKeyId: 
    * `<crypt:signerWithChooserByPrivateKeyId/>` 
    * `<crypt:verifierWithChooserByPublicKeyId/>` 
    * `<crypt:b64SignerWithChooserByPrivateKeyId/>` 
    * `<crypt:b64VerifierWithChooserByPublicKeyId/>`
  * DigitalSignaturesByAlias: 
    * `<crypt:signerWithChooserByAlias/>` 
    * `<crypt:verifierWithChooserByAlias/>` 
    * `<crypt:b64SignerWithChooserByAlias/>` 
    * `<crypt:b64VerifierWithChooserByAlias/>`
  * SymmetricCiphers:
    * `<crypt:symmetricKeyGenerator/>`
    * `<crypt:symmetricCipherer/>`
    * `<crypt:symmetricCiphererWithStaticKey/>`
    * `<crypt:b64SymmetricKeyGenerator/>`
    * `<crypt:b64SymmetricCipherer/>`
    * `<crypt:b64SymmetricCiphererWithStaticKey/>`
  * AsymmetricCiphers:
    * `<crypt:asymmetricCipherer/>`
    * `<crypt:asymmetricCiphererWithChooserByKeyId/>`
    * `<crypt:b64AsymmetricCipherer/>`
    * `<crypt:b64AsymmetricCiphererWithChooserByKeyId/>`
  * MessageDigests
    * `<crypt:digester/>`
  * MessageAuthenticationCodeDigests
    * `<crypt:mac/>`
    * `<crypt:b64Mac/>`